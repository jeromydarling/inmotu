# Copy to .env for local development (gitignored). Vite exposes VITE_* vars to
# the client bundle at build time.

# Client-side Sentry DSN. Safe to expose (DSNs are publishable). Leave unset to
# disable Sentry locally — initSentry() no-ops without it.
VITE_SENTRY_DSN=https://1b1f71df8c411f1a87e4acc111cc652a@o4511311951298560.ingest.us.sentry.io/4511507848953856
